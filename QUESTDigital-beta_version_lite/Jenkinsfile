pipeline {
    agent any
     stages {
         stage('Clone') {
            steps {
                echo 'Cloning repository...'
            }
         }
        stage('Build'){
            steps {
                echo 'Running python file'
                bat 'cd QUESTDIGITAL-BETA_VERSION_LITE && pip install -r requirements.txt'
                bat ' cd QUESTDIGITAL-BETA_VERSION_LITE && python  stefie.py'
            }
        }
        stage('Test'){
            steps {
                echo 'Testing the code'
            }
        }
        stage('Deploy'){
            steps{
                echo 'Deploying to server'
            }
        }
    }
}